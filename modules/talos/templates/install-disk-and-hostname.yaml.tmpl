machine:
  install:
    disk: ${install_disk}
  network:
    hostname: ${hostname}
    interfaces:
      - interface: eth0
        addresses: ["192.168.178.20/24"]
        dhcp: false
    nameservers: ["192.168.178.1"]